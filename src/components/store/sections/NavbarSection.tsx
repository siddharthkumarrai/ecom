import type { SectionRenderProps } from "@/components/store/sections/registry";

export function NavbarSection(_: SectionRenderProps) {
  // Header/nav is already rendered globally by the store layout.
  return null;
}
