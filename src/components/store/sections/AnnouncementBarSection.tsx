import type { SectionRenderProps } from "@/components/store/sections/registry";

export function AnnouncementBarSection(_: SectionRenderProps) {
  // Announcement bar is already rendered globally by the store layout.
  return null;
}
