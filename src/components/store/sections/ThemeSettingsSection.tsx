import type { SectionRenderProps } from "@/components/store/sections/registry";

export function ThemeSettingsSection(_: SectionRenderProps) {
  // Theme settings are applied globally via SiteConfig appearance values.
  return null;
}

