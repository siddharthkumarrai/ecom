import type { SectionRenderProps } from "@/components/store/sections/registry";

export function FooterSection(_: SectionRenderProps) {
  // Footer is already rendered globally by the store layout.
  return null;
}
